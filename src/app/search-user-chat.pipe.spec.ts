import { SearchUserChatPipe } from './search-user-chat.pipe';

describe('SearchUserChatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserChatPipe();
    expect(pipe).toBeTruthy();
  });
});
